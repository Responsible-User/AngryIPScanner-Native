name: CI

on:
  push:
    branches: [master, mac-native, windows-native]
  pull_request:
    branches: [master]

jobs:
  # Go core compiles on all three OSes on every branch. The core is
  # shared, so even a mac-native push must not break Windows (or vice
  # versa). Linux builds remain here as a no-UI smoke test so we catch
  # any accidental platform-specific regressions in the Go code.
  go-test:
    name: Go Tests (${{ matrix.os }})
    strategy:
      fail-fast: false
      matrix:
        os: [macos-latest, ubuntu-latest, windows-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-go@v6
        with:
          go-version: '1.26'

      - name: Run tests
        working-directory: libipscan
        run: go test ./... -v -race -count=1

      - name: Build shared library
        working-directory: libipscan
        run: |
          if [ "$RUNNER_OS" = "macOS" ]; then
            go build -buildmode=c-shared -o libipscan.dylib
          elif [ "$RUNNER_OS" = "Windows" ]; then
            go build -buildmode=c-shared -o libipscan.dll
          else
            go build -buildmode=c-shared -o libipscan.so
          fi
        shell: bash

  go-vet:
    name: Go Vet & Lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-go@v6
        with:
          go-version: '1.26'

      - name: Vet
        working-directory: libipscan
        run: go vet ./...

  # UI builds gated by branch: skip the macOS Swift build on
  # windows-native pushes, skip the Windows WPF build on mac-native
  # pushes. master and PRs run both.
  mac-build:
    name: macOS App Build
    runs-on: macos-latest
    needs: go-test
    if: >
      github.event_name == 'pull_request' ||
      github.ref == 'refs/heads/master' ||
      github.ref == 'refs/heads/mac-native'
    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-go@v6
        with:
          go-version: '1.26'

      - name: Install xcodegen
        run: brew install xcodegen

      - name: Build Go dylib
        working-directory: libipscan
        run: |
          go build -buildmode=c-shared -o ../MacApp/Bridge/libipscan.dylib
          install_name_tool -id "@rpath/libipscan.dylib" ../MacApp/Bridge/libipscan.dylib

      - name: Generate Xcode project
        working-directory: MacApp
        run: xcodegen generate

      - name: Build Swift app
        working-directory: MacApp
        run: >
          xcodebuild -scheme GoNetworkScanner -configuration Debug build
          CODE_SIGN_IDENTITY="-"
          CODE_SIGNING_REQUIRED=NO
          CODE_SIGN_ENTITLEMENTS=""
          ENABLE_HARDENED_RUNTIME=NO
          DEVELOPMENT_TEAM=""

  windows-build:
    name: Windows App Build
    runs-on: windows-latest
    needs: go-test
    if: >
      github.event_name == 'pull_request' ||
      github.ref == 'refs/heads/master' ||
      github.ref == 'refs/heads/windows-native'
    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-go@v6
        with:
          go-version: '1.26'

      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '10.0.x'

      - name: Build Go DLL
        working-directory: libipscan
        run: go build -buildmode=c-shared -o ../WindowsApp/GoNetworkScanner/libipscan.dll
        shell: bash

      - name: Build WPF app
        working-directory: WindowsApp
        run: dotnet build GoNetworkScanner/GoNetworkScanner.csproj -c Release
