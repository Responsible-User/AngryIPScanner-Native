Windows platform fixes: hidden console windows, ARP resolution, pinger compatibility

- Add platform-specific build tags for ping and arp commands
- Hide console windows on Windows (CREATE_NO_WINDOW) for ping/arp
- Use Windows ping flags (-n count, -w timeout) instead of macOS flags
- Parse Windows ping output (Reply from, case-insensitive TTL)
- ARP resolution via cached arp -a with refresh-on-miss strategy
- Fix TCP/UDP pinger error detection for Windows error messages
- Fix ScanProgress JSON field names (active_threads, current_ip)
- Make Preferences window taller, resizable, and scrollable

Co-Authored-By: Claude Opus 4.6 (1M context) <noreply@anthropic.com>
